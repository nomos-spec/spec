{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nomosprotocol.com/schema/rule.schema.json",
  "title": "NOMOS Decision",
  "description": "A single declarative governance decision within a .nomos artifact's logic.decisions array (NOMOS-SPEC-001 §4)",
  "type": "object",
  "required": ["id", "description", "when", "then", "priority"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique decision identifier within this artifact"
    },
    "description": {
      "type": "string",
      "description": "Natural-language description of the decision, derived from the source policy"
    },
    "when": {
      "type": "string",
      "description": "Nomos-Expr v1 condition string (§4.1) — e.g. \"invoice_amount > 25000\", \"always == true\". NOT a condition-tree object."
    },
    "then": {
      "type": "array",
      "items": { "$ref": "#/$defs/outcome" },
      "description": "Outcomes applied when `when` evaluates true, in order"
    },
    "else": {
      "type": "array",
      "items": { "$ref": "#/$defs/outcome" },
      "description": "Outcomes applied when `when` evaluates false. MAY be empty"
    },
    "priority": {
      "type": "integer",
      "description": "Higher value = evaluated first when multiple decisions are eligible (§3.6 logic.resolution)"
    },
    "valid_from": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 UTC — decision is inactive before this instant. Half-open interval: [valid_from, valid_until). NOMOS-SPEC-003 §3."
    },
    "valid_until": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 UTC — decision is inactive at and after this instant. Half-open interval. NOMOS-SPEC-003 §3."
    },
    "provenance": {
      "type": "object",
      "description": "Traceability back to the source document this decision was derived from. RECOMMENDED for every decision derived from a document.",
      "properties": {
        "source_id": { "type": "string" },
        "source_name": { "type": "string" },
        "extractor_version": { "type": "string" },
        "extraction_timestamp": { "type": "string", "format": "date-time" }
      }
    }
  },
  "$defs": {
    "outcome": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["allow", "block", "escalate", "set", "emit", "action"]
        },
        "reason": { "type": "string", "description": "RECOMMENDED for type: block" },
        "escalation_id": { "type": "string", "description": "REQUIRED for type: escalate — references governance.escalations[].id (§3.7)" },
        "variable": { "type": "string", "description": "REQUIRED for type: set" },
        "value": { "description": "REQUIRED for type: set" },
        "action_name": { "type": "string", "description": "REQUIRED for type: action — references execution.actions (§3.8)" }
      }
    }
  }
}
